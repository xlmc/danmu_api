# Changelog

## Unreleased

- 为自动匹配映射增加发布组条件：支持 `{[group=字幕组]}`（含多个组），文件名中的显式发布组优先命中精确规则，目标失败或无精确规则时回退通用规则。
- 解析文件名时提取前置标签和带 `SxxExx` 的 `-Group`/`@Group` 后缀；画质、编码、平台、年份等技术标记不会被当成发布组，标题末尾的普通连字符不会被误删。
- 保留匹配优化分支的 O(1) 标题/季度索引，并为分组规则使用独立索引，避免每次请求扫描全部映射规则。
- 增加发布组优先级、通用回退、平台/画质排除和标题边界测试。
