# 更新日志 (Changelog)

本项目所有用户可见的重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **远程季集表运行时接入**：远程 `season-candidates.txt` 作为有限范围季集规则加载到本机缓存；标题表失败后支持标题+季集组合候选，实际匹配成功即停止。
- **远程季集状态日志**：季集表的缓存加载、更新、跳过开放规则及失败状态统一写入 `[remote-mapping] [remote-season]` 日志，可通过远程映射日志查看。
- **配置变更即时生效**：修改 `AUTO_MATCH_MAPPING_TABLE_URL` 后热加载会重新初始化全局配置并立即检查/下载季集表，不必重启服务。

- **远程剧名映射表手动更新**：设置页新增「Remote Mapping Update（远程映射表更新）」独立配置项，提供「立即更新」按钮，可随时手动拉取远程映射表
  - 手动更新仅尝试 1 次，5 秒超时，失败时保留旧缓存，不影响现有映射
  - 更新结果（成功 N 条 / 失败）以内联状态文字显示在按钮下方，不再弹窗打扰
- **远程映射专用日志**：新增远程映射独立日志缓冲区与接口
  - 日志上限 500 条，超过后淘汰最旧记录，与系统主日志保持一致的先进先出机制
  - 通过设置页或 `/api/logs/remote-mapping` 查看匹配命中/失败详情

### 变更

- **移除 `TITLE_MAPPING_TABLE_URL_CACHE_MINUTES` 配置**：不再需要手动配置缓存分钟数
  - 程序首次启动联网下载并保存本地缓存，之后匹配只读内存/磁盘缓存，不因 TTL 联网
  - 每天北京时间 05:30 自动更新远程映射表，单次更新最多重试 5 次（间隔 60 秒），失败保留旧缓存
- **文件名发布标签清理增强**：自动匹配前清除文件名开头的 `[WEB-DL]`、`[Group]`、`[1080p]`、`[x264]` 等发布/画质/编码/字幕标签及视频扩展名，避免杂质进入剧名导致映射失配
- **剧名映射匹配优化**：
  - 本地映射表每次实时读取最新配置，修改/清空后立即生效，无需重启
  - 本地与远程表均建立「精确 / 分隔符归一化 / 紧凑」三级查询索引，`Monster.Island` ≡ `Monster Island` ≡ `monsterisland` 均可命中
  - 修复 URL 切换/配置重载时旧远程键可能残留为本地规则的问题，杜绝本地表污染

### 文档

- `title-mapping-url-util.js` 与 `dandan-api.js` 文件名解析部分补充面向初学者的详细中文注释
